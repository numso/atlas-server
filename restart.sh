#!/bin/sh

if [ $(ps -e -o uid,cmd | grep $UID | grep node | grep -v grep | wc -l | tr -s "\n") -eq 0 ]
then
        export PATH=/usr/local/bin:$PATH
        cd /opt/nodejs/atlas
        forever start -a --uid atlas -c "npm start" ./
fi
